// ** Icon imports
import { HomeOutline } from 'mdi-material-ui';

export const menuConfig = (): any => {
  return [
    {
      title: 'Dashboard',
      icon: HomeOutline,
      path: '/dashboard'
    }
  ];
};
